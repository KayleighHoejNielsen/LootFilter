import * as React from "react";
import GenerateButton from "../molecules/generateButton";

export default class GenerateSection extends React.Component<{}> {
    render() {
        return (
            <GenerateButton />
        );
    }
}